name: CI

on:
  push:
  schedule:
     - cron:  '0 */1 * * *'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: ml-evs/autovalidate_optimade@master
      with: 
          host_to_validate: http://odbx.science
